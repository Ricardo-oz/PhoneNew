<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.AnonymousPhone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/dark_primary_color</item>
        <item name="colorAccent">@color/accent_color</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:textColor">@color/text_icon</item>

        <!-- Background, icons, and divider colors -->
        <item name="android:colorBackground">@color/light_primary_color</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="dividerColor">@color/divider_color</item>
        <item name="android:colorPrimary">@color/primary_color</item>
        <item name="android:textColorLink">@color/accent_color</item>


    </style>

    <!-- Derived theme with additional customization -->
    <style name="Theme.AnonymousPhone" parent="Base.Theme.AnonymousPhone">
        <!-- Customize specific elements for light and dark themes here if needed -->
    </style>

</resources>
